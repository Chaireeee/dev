{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Formation</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Student_escp</th>
                <td>{{ entity.studentescp }}</td>
            </tr>
            <tr>
                <th>Student_exte</th>
                <td>{{ entity.studentexte }}</td>
            </tr>
            <tr>
                <th>Executive</th>
                <td>{{ entity.executive }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Nameen</th>
                <td>{{ entity.nameEn }}</td>
            </tr>
            <tr>
                <th>Concept</th>
                <td>{{ entity.concept }}</td>
            </tr>
            <tr>
                <th>Concepten</th>
                <td>{{ entity.conceptEn }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('formation') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('formation_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
