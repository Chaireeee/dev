{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Formation edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('formation') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>

     <a href="{{ path('chaire_modify_photo_return',{'id':entity.logo.id}) }}"> edit picture</a>
{% endblock %}
